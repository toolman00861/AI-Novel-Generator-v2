<UserControl x:Class="AINovelStudio.Views.CharacterDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 页面标题和操作栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 页面标题改为统一标题样式 -->
            <TextBlock Grid.Column="0" Text="人物设计" Style="{StaticResource TitleStyle}"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Novels}" SelectedItem="{Binding SelectedNovel}" 
                        DisplayMemberPath="Title" MinWidth="180" Margin="0,0,10,0" Style="{StaticResource ModernComboBoxStyle}"/>
                <Button Content="新建角色" Command="{Binding CreateCharacterCommand}" 
                      Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="刷新" Command="{Binding RefreshCommand}" 
                      Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Grid>

        <!-- 角色列表 -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="角色列表" Style="{StaticResource SubtitleStyle}" Margin="0,0,0,10"/>
                
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Characters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Padding="10" Margin="0,0,10,10" MinWidth="120" Cursor="Hand">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" 
                                                Command="{Binding DataContext.SelectCharacterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                </Border.InputBindings>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource SubtitleStyle}" TextAlignment="Center"/>
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource SecondaryTextStyle}" 
                                             TextAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="120"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- 角色详情编辑区域 -->
        <Grid Grid.Row="2" Visibility="{Binding IsCharacterSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：基本信息 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 标题 -->
                    <Border Grid.Row="0" Background="{StaticResource HoverBrush}" Padding="15,10">
                        <TextBlock Text="基本信息" Style="{StaticResource SubtitleStyle}"/>
                    </Border>

                    <!-- 表单内容 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel>
                            <!-- 角色名称 -->
                            <TextBlock Text="角色名称" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SelectedCharacter.Name, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

                            <!-- 角色描述 -->
                            <TextBlock Text="角色描述" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SelectedCharacter.Description, UpdateSourceTrigger=PropertyChanged}" 
                                   TextWrapping="Wrap" AcceptsReturn="True" Height="80"
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

                            <!-- 性格特点 -->
                            <TextBlock Text="性格特点" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SelectedCharacter.Personality, UpdateSourceTrigger=PropertyChanged}" 
                                   TextWrapping="Wrap" AcceptsReturn="True" Height="100"
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

                            <!-- 背景故事 -->
                            <TextBlock Text="背景故事" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SelectedCharacter.Background, UpdateSourceTrigger=PropertyChanged}" 
                                   TextWrapping="Wrap" AcceptsReturn="True" Height="120"
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

                            <!-- 说话风格 -->
                            <TextBlock Text="说话风格" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SelectedCharacter.SpeakingStyle, UpdateSourceTrigger=PropertyChanged}" 
                                   TextWrapping="Wrap" AcceptsReturn="True" Height="80"
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

                            <!-- 标签 -->
                            <TextBlock Text="标签（用逗号分隔）" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CharacterTagsText, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- 操作按钮 -->
                    <Border Grid.Row="2" Background="{StaticResource HoverBrush}" Padding="15">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="保存" Command="{Binding SaveCharacterCommand}" 
                                  Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                            <Button Content="删除" Command="{Binding DeleteCharacterCommand}" 
                                  Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                            <Button Content="AI优化" Command="{Binding OptimizeCharacterCommand}" 
                                  Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- 右侧：AI生成和预览 -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 标题 -->
                    <Border Grid.Row="0" Background="{StaticResource HoverBrush}" Padding="15,10">
                            <TextBlock Text="AI助手" Style="{StaticResource SubtitleStyle}"/>
                        </Border>

                    <!-- AI生成选项 -->
                    <Border Grid.Row="1" Background="{StaticResource HoverBrush}" Padding="15">
                        <StackPanel>
                            <TextBlock Text="AI生成选项" Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <RadioButton Content="生成性格" IsChecked="{Binding IsGeneratePersonality}" 
                                           GroupName="AIGenerate" Margin="0,0,15,0"/>
                                <RadioButton Content="生成背景" IsChecked="{Binding IsGenerateBackground}" 
                                           GroupName="AIGenerate" Margin="0,0,15,0"/>
                                <RadioButton Content="生成对话" IsChecked="{Binding IsGenerateDialogue}" 
                                           GroupName="AIGenerate"/>
                            </StackPanel>
                            <TextBox Text="{Binding AIPrompt, UpdateSourceTrigger=PropertyChanged}" 
                                   Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                                   Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,10"/>
                            <Button Content="AI生成" Command="{Binding GenerateWithAICommand}" 
                                  Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>

                    <!-- AI生成结果 -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel>
                            <TextBlock Text="生成结果" Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
                            <TextBox Text="{Binding AIGeneratedContent}" IsReadOnly="True" 
                                    TextWrapping="Wrap" Style="{StaticResource ModernTextBoxStyle}" MinHeight="200"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- 应用按钮 -->
                    <Border Grid.Row="3" Background="{StaticResource HoverBrush}" Padding="15">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="应用到角色" Command="{Binding ApplyAIContentCommand}" 
                                  Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"
                                  IsEnabled="{Binding HasAIContent}"/>
                            <Button Content="复制内容" Command="{Binding CopyAIContentCommand}" 
                                  Style="{StaticResource SecondaryButtonStyle}"
                                  IsEnabled="{Binding HasAIContent}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- 空状态提示 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="40" Visibility="{Binding IsCharacterSelected, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="📝" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="选择一个角色开始编辑" FontSize="16" HorizontalAlignment="Center" 
                         Foreground="Gray"/>
                <TextBlock Text="或者创建一个新角色" FontSize="14" HorizontalAlignment="Center" 
                         Foreground="Gray" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>