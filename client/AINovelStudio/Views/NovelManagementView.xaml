<UserControl x:Class="AINovelStudio.Views.NovelManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">

    <Grid Margin="24" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform Y="24"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         To="1"
                                         Duration="{StaticResource NormalDuration}"
                                         EasingFunction="{StaticResource EaseInOut}"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                         To="0"
                                         Duration="{StaticResource NormalDuration}"
                                         EasingFunction="{StaticResource EaseOut}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和操作栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="📚 小说管理"
                           Style="{StaticResource TitleStyle}"/>
                <TextBlock Style="{StaticResource SecondaryTextStyle}">
                    <Run Text="当前共 "/>
                    <Run Text="{Binding NovelCount, Mode=OneWay}"/>
                    <Run Text=" 部作品"/>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="🔄 刷新"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,12,0"/>
                <Button Content="+ 新建小说"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CreateNovelCommand}"/>
            </StackPanel>
        </Grid>

        <!-- 小说列表 -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      PanningMode="VerticalFirst">
            <ItemsControl ItemsSource="{Binding Novels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}"
                                Margin="0,0,0,20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 标题与状态 -->
                                <TextBlock Grid.Row="0"
                                           Grid.Column="0"
                                           Text="{Binding Title}"
                                           Style="{StaticResource SubtitleStyle}"/>

                                <Border Grid.Row="0"
                                        Grid.Column="1"
                                        Padding="10,4"
                                        CornerRadius="12"
                                        HorizontalAlignment="Right">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#4B74FF"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Draft">
                                                    <Setter Property="Background" Value="#A3AED0"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                    <Setter Property="Background" Value="#4B74FF"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Background" Value="#34C759"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Published">
                                                    <Setter Property="Background" Value="#F97316"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}"
                                               Foreground="White"
                                               FontSize="12"
                                               FontWeight="SemiBold"/>
                                </Border>

                                <!-- 描述 -->
                        <TextBlock Grid.Row="1"
                                   Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Text="{Binding Description}"
                                   Style="{StaticResource BodyStyle}"
                                   Margin="0,10,0,0"/>

                                <!-- 标签 -->
                                <ItemsControl Grid.Row="2"
                                              Grid.Column="0"
                                              Grid.ColumnSpan="2"
                                              ItemsSource="{Binding Tags}"
                                              Margin="0,12,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#EEF2FF"
                                                    BorderBrush="#DDE3FF"
                                                    BorderThickness="1"
                                                    CornerRadius="12"
                                                    Padding="8,4"
                                                    Margin="0,0,6,6">
                                                <TextBlock Text="{Binding}"
                                                           FontSize="12"
                                                           Foreground="#38405F"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- 底部信息与操作 -->
                                <Grid Grid.Row="3"
                                      Grid.Column="0"
                                      Grid.ColumnSpan="2"
                                      Margin="0,18,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0,0,16,0">
                                            <TextBlock Text="🗓"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Style="{StaticResource SecondaryTextStyle}">
                                                <Run Text="创建于 "/>
                                                <Run Text="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd}}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📄"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Style="{StaticResource SecondaryTextStyle}">
                                                <Run Text="{Binding Chapters.Count, Mode=OneWay}"/>
                                                <Run Text=" 个章节"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal">
                                        <Button Content="📝 编辑"
                                                Style="{StaticResource PrimaryButtonStyle}"
                                                Command="{Binding DataContext.EditNovelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,10,0"/>
                                        <Button Content="📖 章节"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding DataContext.ManageChaptersCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,10,0"/>
                                        <Button Content="🗑️ 删除"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding DataContext.DeleteNovelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
