<UserControl x:Class="AINovelStudio.Views.NovelManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题和操作栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="📚 小说管理" Style="{StaticResource TitleStyle}"/>
                <TextBlock Text="{Binding NovelCount, StringFormat='共 {0} 部小说'}" Style="{StaticResource SecondaryTextStyle}" Margin="20,0,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ 新建小说" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CreateNovelCommand}"/>
                <Button Content="🔄 刷新" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>
        
        <!-- 小说列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Novels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 小说标题 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title}" Style="{StaticResource SubtitleStyle}"/>
                                
                                <!-- 状态标签 -->
                                <Border Grid.Row="0" Grid.Column="1"
                                       Background="#3498DB" 
                                       CornerRadius="12" 
                                       Padding="8,4">
                                    <TextBlock Text="{Binding Status}" 
                                              Foreground="White" 
                                              FontSize="12"/>
                                </Border>
                                
                                <!-- 描述 -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Description}" Style="{StaticResource BodyStyle}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                
                                <!-- 标签 -->
                                <ItemsControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                             ItemsSource="{Binding Tags}"
                                             Margin="0,8,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#ECF0F1" 
                                                   CornerRadius="10" 
                                                   Padding="6,3" 
                                                   Margin="0,0,5,5">
                                                <TextBlock Text="{Binding}" 
                                                          FontSize="12" 
                                                          Foreground="#34495E"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <!-- 操作按钮和信息 -->
                                <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='创建于 {0:yyyy-MM-dd}'}" Style="{StaticResource SecondaryTextStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Chapters.Count, StringFormat='• {0} 章节'}" Style="{StaticResource SecondaryTextStyle}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Button Content="📝 编辑" 
                                               Style="{StaticResource PrimaryButtonStyle}"
                                               Command="{Binding DataContext.EditNovelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"/>
                                        <Button Content="📖 章节" 
                                               Style="{StaticResource SecondaryButtonStyle}"
                                               Command="{Binding DataContext.ManageChaptersCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"/>
                                        <Button Content="🗑️ 删除" 
                                               Style="{StaticResource SecondaryButtonStyle}"
                                               Command="{Binding DataContext.DeleteNovelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>